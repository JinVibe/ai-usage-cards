{
  "name": "ai-usage-cards",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub README card for AI-assisted development outcomes — embeddable from a single URL",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "vercel dev"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
